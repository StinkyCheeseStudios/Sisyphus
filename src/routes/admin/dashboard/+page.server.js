//import lib

import { canAssignShift, getWorkerScheduledHours, getScheduleStats, generateSchedule } from '$lib/function/scheduler.js';


export const actions = {
    placeholder: () => {
        console.log("it works");
    }

}